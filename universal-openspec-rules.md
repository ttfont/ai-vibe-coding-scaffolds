# Universal OpenSpec Rules

## Fast Start

如果你只想知道“跨项目最值得保留的 OpenSpec 规则是什么”，先记住这 8 条：

1. 所有产出物统一语言，避免中英文规范混写。
2. 先契约后实现，行为变化先写 OpenSpec。
3. `propose` 先分析现状，再给方案，再拆任务。
4. `apply` 严格按已确认变更实施，不扩大范围。
5. 普通业务改动和框架/鉴权改动必须分流处理。
6. 接口、字段、错误码、鉴权变化必须说明兼容性和回滚方式。
7. 默认最小改动，禁止无关重构。
8. 完成前必须有验证结论，而不是只说“已完成”。

## When To Read More

只有在这些场景，才继续往后读：

- 你要设计一个可复用的 OpenSpec 模板
- 你要给新项目落一套规则
- 你想判断哪些内容应该放进通用模板，哪些只该放项目配置
- 你发现轻量模板不足以约束 AI 输出，想增强约束密度

## Core Rules

### 1. 文档分层

最适合跨项目复用的第一层规则，不是具体技术细节，而是分层方式：

- `AGENTS.md` 负责工作规则、读取顺序、协作纪律、验证口径
- `project.md` 负责稳定背景、技术栈、架构、模块边界、领域术语
- `openspec/config.yaml` 负责给 AI 的高密度生成约束
- `openspec/changes/*` 负责单次变更过程
- `openspec/specs/*` 负责稳定契约

这条几乎适用于所有项目，优先模板化。

### 2. OpenSpec 工作流

所有项目都建议保留以下最小工作流：

- `propose`：先分析现状，再给方案，再生成 proposal / design / tasks
- `apply`：严格按已确认变更实施，不扩大需求范围
- `explore`：只做阅读、分析、定位、建议
- `archive`：只沉淀稳定、长期可复用的规则

如果一个项目经常出现“AI直接编码但方向错了”的问题，应该优先强化这里，而不是先加更多编码细节。

### 3. 范围控制

跨项目最通用也最重要的约束之一：

- 不因为“以后可能会用到”主动扩展范围
- 不重构无关代码
- 不在 `apply` 阶段新增未确认需求
- 不把一次性业务要求写成全局规范

这类规则对任何技术栈都成立。

### 4. 兼容性规则

只要项目有外部接口、前后端契约或跨模块依赖，就建议模板化：

- 接口路径变化必须显式说明影响范围
- 字段语义变化必须说明兼容策略
- 错误码变化必须说明上游/下游影响
- 鉴权方式变化必须说明迁移和回滚方式
- 默认不随意改变已有公共返回结构

### 5. 验证纪律

很多项目缺的不是实现规则，而是交付规则：

- 完成前必须说明验证方式
- 完成前必须说明验证结果
- `tasks.md` 只能在真实完成后勾选
- 联调类需求不能只以“本地编译通过”代替完成

## Split Rules

### 普通业务规则

这类规则很适合做通用模板，但不要写死成某个项目专属分层名：

- 普通业务优先遵循项目既有分层结构
- 接口默认统一返回格式
- 列表接口默认分页
- Controller 只负责接参、校验、权限、调用 Service、返回结果
- Service 承载业务规则、事务和编排
- 复杂查询放到既有查询层，而不是散落在业务代码里

### Framework Exceptions

这部分特别值得做成通用模板，因为很多项目都需要“分流规则”：

- JWT / 鉴权
- Spring Security / Shiro / Sa-Token 等安全框架
- Token 生成、校验、刷新、缓存
- 登录过滤器、权限拦截器、网关过滤器
- 全局异常、缓存封装、通用配置

通用处理原则：

- 不强行套普通 CRUD 模板
- 必须先分析现有链路
- 必须按最小改动处理
- 必须说明影响范围、兼容策略和回滚方式

这类规则建议所有后端项目都保留。

## Backend Universal Rules

这部分是跨 Java / Go / Node / Python 后端项目都常见的通用约束：

- 保持项目既有目录与分层，不发明无意义新层级
- 控制层不堆业务逻辑
- 异常不能吞，日志要保留关键上下文
- 日志不得泄露密码、Token、密钥、手机号完整值等敏感信息
- 数据访问层要和项目既有方式保持一致
- 行为变化优先更新契约，再改实现

如果是 Java / Spring 项目，还可以进一步细化：

- 统一响应模型
- 分页对象规范
- Mapper XML / Repository / Query 层职责
- 事务边界

## Frontend Universal Rules

这部分也适合通用化，但应该写成“原则”，不要写死成单一框架实现：

- 页面、组件、状态、路由、API 封装按业务域组织
- 优先复用共享组件、样式变量、请求封装和布局容器
- UI 改动默认不顺手改变业务逻辑和文案语义
- 新增接口路径时同步检查代理、网关、环境变量和构建配置
- 默认不在前端持久化敏感明文
- 前端入口和后端契约必须一致

如果你希望模板有前端默认值，可以提供基线，例如：

- React 18 + TypeScript 5 + Vite

但这属于“默认基线”，不是“强制通用真理”。

## API Universal Rules

最适合跨项目复用的接口约束通常是：

- URI 风格保持一致
- 列表默认分页
- 输入默认校验
- 不随意新增全量查询接口
- 不随意修改已有接口路径
- 不随意修改公共响应结构
- 面向外部系统的行为变化必须先在 proposal/design 中明确

是否要求全小写、是否禁止中横线、是否固定 REST 风格，这些要看团队偏好，可作为“可选增强规则”。

## Database Universal Rules

数据库层真正适合通用化的内容通常比想象中少，保留原则即可：

- 保持项目既有命名风格
- 默认逻辑删除策略与现有项目一致
- 索引只加在高频查询字段，不滥用
- 关联关系保持统一表达方式
- DDL 注释和初始化数据写法保持团队一致

具体到 PostgreSQL / MySQL / Oracle 的字段类型、主键策略、表前缀，不建议直接上升成 universal 规则。

## AI Prompt Strengthening

如果你发现“必须提示词写到位才能执行正确”，通常说明需要从 full config 里上升这些规则：

- 明确区分普通业务和 framework_exceptions
- 明确 Controller、Service、DAO、VO、Query、Convert 的责任边界
- 明确列表默认分页、禁止全量查询
- 明确统一响应模型
- 明确 URI 规范
- 明确敏感信息、日志、异常和兼容性要求
- 明确完成前检查清单

也就是说，真正能提升执行正确率的，往往不是“再多讲一点项目背景”，而是“把容易跑偏的决策点提前写死”。

## What To Template

优先放进通用模板的内容：

- 文档分层
- OpenSpec 生命周期
- 范围控制
- 兼容性要求
- 验证纪律
- framework_exceptions 分流规则
- 日志/异常/敏感信息原则
- 前端代理/入口一致性原则

## What Not To Template

不建议直接做成 universal 规则的内容：

- 项目专属模块名
- 项目专属包名或命名空间
- 业务域专有名词
- 具体类名、具体配置文件名
- 数据库表前缀
- 具体库版本
- 具体白名单文件名

这些内容更适合放进项目级 `project.md` 或项目级 `openspec/config.yaml`。

## Escalation Path

如果轻量模板效果不好，建议按这个顺序增强，而不是一次性塞满所有细节：

1. 先补 `framework_exceptions`
2. 再补统一响应、分页、兼容性、范围控制
3. 再补日志、异常、敏感信息和验证清单
4. 最后才补项目专属分层和命名细节

这样更符合渐进披露，也更便于跨项目复用。

## Appendix

### Recommended Minimal Universal Set

如果你只想保留最小通用集合，建议至少有：

- 文档分层
- OpenSpec 四阶段工作流
- 范围控制
- 兼容性声明
- 验证纪律
- framework_exceptions 分流
- 日志与敏感信息规则

### Recommended Strong Universal Set

如果你想让 AI 更不容易跑偏，可以在最小集合上再增加：

- Controller / Service / 数据访问层职责边界
- 默认分页规则
- 统一响应规则
- URI 风格规则
- 完成前检查清单
